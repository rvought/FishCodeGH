function out = grid_xyoverlap(in, sd, win, overlap)
% Usage: out = grid_xyoverlap(in, sd, win, overlap)
% This calculates the pairwise and total overlaps for "territories" in the
% grid. The goal is to assess how territorial the fish are over time.
% 